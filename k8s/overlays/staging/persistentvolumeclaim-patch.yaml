apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    app: mssql-data
  annotations:
    storageClassName: azure-disk
  name: mssql-data
  namespace: relibank
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: accounts-data
  namespace: relibank
  labels:
    app: accounts-db
  annotations:
    storageClassName: azure-disk
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
