apiVersion: v1
kind: Service
metadata:
  name: bill-pay-service
  namespace: relibank
  labels:
    app: bill-pay-service
spec:
  ports:
    - name: "5000"
      port: 5000
      targetPort: 5000
  selector:
    app: bill-pay-service
