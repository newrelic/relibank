apiVersion: v1
kind: Secret
metadata:
  name: relibank-secrets
  namespace: relibank
type: Opaque
stringData:
  # Database passwords
  DB_PASSWORD: "PostgresPass123!"
  SA_PASSWORD: "SqlServer2024#"
  MSSQL_SA_PASSWORD: "SqlServer2024#"

  # Azure Communication Services for notifications
  AZURE_ACS_CONNECTION_STRING: "endpoint=https://demo.communication.azure.com/;accesskey=demokey123"

  # Kafka credentials
  kafka-password: "KafkaPass123!"

  # Application secrets
  jwt-secret-key: "jwt-super-secret-key-2024!"
  encryption-key: "encryption-key-256bit-strong!"

  # External service API keys (demo values)
  notification-api-key: "demo-notification-api-key"
  payment-gateway-secret: "demo-payment-gateway-secret"

  # Monitoring
  newrelic-license-key: "demo-newrelic-license-key"
