# Relibank Test Suite Requirements
# Install with: pip install -r requirements.txt

# Core testing framework
pytest>=7.4.0
pytest-xdist>=3.3.0
pytest-timeout>=2.1.0

# HTTP requests
requests>=2.31.0

# Database connectivity (for transaction validation tests)
pyodbc>=5.0.0
# Browser automation for microfrontend tests
selenium>=4.0.0
# Kubernetes API client (for infra resilience tests, e.g. test_kafka_zookeeper_resilience.py)
kubernetes>=28.1.0
